# Vendor patch ledger

Version-specific vendor patches generated from the current local bytes. Each manifest entry contains the full UTF-8 replacement text and its SHA-256 precondition; `apply_patches.py` dry-run validates scope and drift before any write.

- Computer Use: use the host CUA route and retain Sky only when callable
- Spreadsheets: plan fallback when update_plan is absent
- Deep Research: capability-aware planning and deliverables
- OpenAI Docs: local diagnosis before current official facts
- Figma library: focused route for a component in an existing library
- Figma component reference: align focused work with existing dependencies
- Figma SwiftUI: Figma-specific activation
- Figma use: risk-based call sizing and serialized mutations
- Figma gotchas: serialize multi-page mutations
- Product Design ideate: requested or uncertainty-sized image count
- URL to code: capture and reproduce requested important states
- Sites: optional asset delegation
- Documents: render layout-affecting edits and perform final QA

Manifest: the local `manifest.json` (published as [focused patches](../patches/README.md)) (13 guarded entries).

## Applied and verified

All 13 files were applied with guarded backups. Final metadata, 35-file
personal/vendor parity, and idempotent dry-runs pass; 125 relative file links
resolve across both patch sets. Final review retained desktop/mobile source
coverage for URL cloning and fixed a Figma heading link.

Receipts:
- Initial vendor application: `C:/Users/abdul/AppData/Local/Temp/skills-audit-backup-1mhw2m07/files/receipt.json`
- Figma link correction: `C:/Users/abdul/AppData/Local/Temp/skills-audit-backup-tobmx_7d/files/receipt.json`

The main manifest contains the final desired files with their original baseline
hashes. `link-correction.json` records the intermediate correction. These are
version-specific patches; vendor updates can replace them. Review a new baseline
before reapplying after an upstream update.
