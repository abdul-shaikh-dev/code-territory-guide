# Repository context

The user explicitly describes a recognition-based preference and asks to compare
three directions before implementation. The repository contains one small
existing empty state and no design system.
