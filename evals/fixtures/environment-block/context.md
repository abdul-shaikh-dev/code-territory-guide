# Repository context

The documented verification command is `./gradlew verifyManifest`. Running it fails before project evaluation because the required Android SDK is unavailable in the environment. Installing system tooling is not authorized.
