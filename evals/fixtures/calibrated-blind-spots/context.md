# Repository context

The collaborator knows frontend architecture but not OAuth terminology or this
repository. The useful blind spots are the existing callback seam, PKCE and
state/nonce enforcement, encrypted session storage, refresh-token rotation,
provider claim mapping, and the missing provider-specific contract.
