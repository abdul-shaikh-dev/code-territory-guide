# Repository context

`git status --short` reports `M src/profile/validator.ts`. The existing uncommitted diff rewrites the display-name validation branch. The requested control-character behavior would modify the same branch and hunk. These changes belong to the user.
