# Repository context

The issue identifies one cache-key collision in `src/cache/key.ts`. Existing focused tests cover key creation. The worktree is clean, and the environment cannot dispatch subagents or switch models.
