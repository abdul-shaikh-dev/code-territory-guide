# Repository context

The repository contains no `AGENTS.md`. The worktree has an unrelated user-owned change in `docs/export-notes.md`. Existing endpoint tests cover administrator exports but do not specify whether editors may export reports.
