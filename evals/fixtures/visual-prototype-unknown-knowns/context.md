# Visual prototype fixture

The repository contains a production dashboard and an existing Markdown brief.
The user has described a recognition-based layout preference that should be
resolved before production implementation.
