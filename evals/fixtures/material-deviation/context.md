# Repository context

The written plan says to reject non-string input, but the current implementation
and focused test preserve dictionary input. The user authorized the conservative
compatible option and requested durable deviation notes.
