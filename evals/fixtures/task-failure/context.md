# Repository context

Before the task, all slug-normalizer tests pass. After the proposed patch, the focused test for repeated underscores fails. The failure points to the changed normalization branch.
